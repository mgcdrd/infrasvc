---
# Base config (driver, provisioner, verifier, sequence) comes from
# ../../../../.config/molecule/config.yml via MOLECULE_BASE_CONFIG.
#
# This scenario stands up the full native stack on one host: the
# mgcdrd.infrabase.postgresql role in prepare.yml, then the keycloak
# role in native mode against it.

platforms:
  - name: kc-native-rocky9
    image: geerlingguy/docker-rockylinux9-ansible:latest
    command: /usr/lib/systemd/systemd
    systemd: true
    privileged: true
    cgroupns_mode: host
    pre_build_image: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw

  - name: kc-native-debian13
    image: geerlingguy/docker-debian13-ansible:latest
    command: /lib/systemd/systemd
    systemd: true
    privileged: true
    cgroupns_mode: host
    pre_build_image: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
