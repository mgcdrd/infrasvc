# {{ ansible_managed }}
# mgcdrd.infrasvc.keycloak (native) — bootstrap admin, first start only.
# Keycloak drops the bootstrap admin once a permanent admin exists.
KC_BOOTSTRAP_ADMIN_USERNAME={{ keycloak_admin_user }}
KC_BOOTSTRAP_ADMIN_PASSWORD={{ keycloak_admin_password }}
