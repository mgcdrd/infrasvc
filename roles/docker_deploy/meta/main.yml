---

galaxy_info:
  role_name: docker_deploy
  author: mgcdrd
  description: Deploys Docker stacks (swarm) or standalone Compose projects from a variable list — generic, not tied to a deployment.
  license: GPL-3.0-or-later
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
    - name: EL
      versions:
        - "9"
        - "10"

dependencies: []
